+package org.eolang.io
+rt jvm org.eolang:eo-runtime:0.1.18

[] > stdin
  # Consumes only one line from the standard input stream
  # Datarizes to an error object if there is no line to consume
  [] > nextLine /string

  # Consumes all data from the standard input stream
  [] > @ /string
