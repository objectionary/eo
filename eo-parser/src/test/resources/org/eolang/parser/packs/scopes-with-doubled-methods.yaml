xsls:
  - /org/eolang/parser/wrap-method-calls.xsl
tests:
  - //o[@base='.foo' and count(o[starts-with(@base,'synth-')])=2]
  - //o[@base='.bar' and count(o)=2 and starts-with(@name,'synth-')]
eo: |
  [] > aliases
    eq. > @
      (1.bar 2).foo (3.bar 4)