line: 4
eo: |
  # At least 64 symbols length mandatory comment should be place before abstract object
  [] > test /int
    # At least 64 symbols length mandatory comment should be place before abstract object
    [] > inner
