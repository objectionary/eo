line: 19
eo: |
  # This is the default 64+ symbols comment in front of abstract object
  [] > main
    # This is the default 64+ symbols comment in front of abstract object
    [] > x
  
    # This is the default 64+ symbols comment in front of abstract object
    [] > y

    # This is the default 64+ symbols comment in front of abstract object
    [] > z

    # This is the default 64+ symbols comment in front of abstract object
    [] > m
    x > a
  
    # This is the default 64+ symbols comment in front of abstract object
    [] > o
    
    o > b
