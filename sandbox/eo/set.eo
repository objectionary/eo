+package sandbox
+alias merge sandbox.merge
+alias slice sandbox.slice

[arr element position] > set
  merge > @
    append.
      slice
        arr
        0
        position
      element
    slice
      arr
      add.
        position
        1
      arr.length
