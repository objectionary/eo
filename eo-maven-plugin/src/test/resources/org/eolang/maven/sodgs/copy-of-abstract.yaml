locators:
  - .foo .a .π
  - .foo .b .α0
  - .foo .b .π
  - .foo .b !.σ
  - .foo .b .π .φ .π .β .plus .ε .β .a .ε
eo: |
  # This is the default 64+ symbols comment in front of named abstract object.
  [] > foo
    5 > a
    bar 8 > b
  # This is the default 64+ symbols comment in front of named abstract object.
  [c] > bar
    ^.a.plus c > @
