+package org.eolang.custom
+version 0.0.0

[] > creates-object
  QQ.rust > r
    """
    use rand::Rng;

    pub fn foo() -> Result<u32, u32> {
      let mut rng = rand::thread_rng();
      print!("Hello world");
      let i = rng.gen::<u32>();
      Ok(i)
    }
    """
    *
      "rand= \"0.5.5\""
  TRUE > @

