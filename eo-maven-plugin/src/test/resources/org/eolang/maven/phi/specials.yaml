eo: |
  # This is the default 64+ symbols comment in front of named abstract object.
  [] > main
    ^.x > x
    y.& > h
    $.a > a
    < > vtx
    ^.< > self
    @.@ > phi
phi: |
  {
    main ↦ ⟦
      x ↦ ξ.ρ.x,
      h ↦ Φ.org.eolang.y.σ,
      a ↦ ξ.a,
      vtx ↦ ξ.ν,
      self ↦ ξ.ρ.ν,
      phi ↦ ξ.φ.φ
    ⟧
  }