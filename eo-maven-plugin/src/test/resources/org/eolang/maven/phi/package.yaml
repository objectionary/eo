eo: |
  +package foo.bar.baz
  
  # This is the default 64+ symbols comment in front of named abstract object.
  [] > main
    stdout > @
phi: |
  {
    foo ↦ ⟦
      bar ↦ ⟦
        baz ↦ ⟦
          main ↦ ⟦
            φ ↦ Φ.org.eolang.stdout
          ⟧,
          λ ⤍ Package
        ⟧,
        λ ⤍ Package
      ⟧,
      λ ⤍ Package
    ⟧
  }