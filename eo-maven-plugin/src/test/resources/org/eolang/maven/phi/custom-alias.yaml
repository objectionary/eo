eo: |
  +package foo.bar
  +alias com.yegor256.x
  
  # This is the default 64+ symbols comment in front of named abstract object.
  [y] > main
    x y > z
phi: |
  {
    foo ↦ ⟦
      bar ↦ ⟦
        main ↦ ⟦
          y ↦ ∅,
          z ↦ Φ.com.yegor256.x(
            α0 ↦ ξ.y
          )
        ⟧,
        λ ⤍ Package
      ⟧,
      λ ⤍ Package
    ⟧
  }