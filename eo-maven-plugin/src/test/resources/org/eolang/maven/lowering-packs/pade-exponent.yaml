# SPDX-FileCopyrightText: Copyright (c) 2016-2026 Objectionary.com
# SPDX-License-Identifier: MIT
---
# D
input: |
  [] > foo
    [x] > pade
      div. > @
        plus.
          times.
            plus.
              x
              6
            x
          12
        plus.
          times.
            plus.
              x
              -6
            x
          12
    pade 0.5 > @
prelude:
  number.eo: |
    [@] > number
      [] > div /Q.number
        ? > ^
        ? > x /Q.number
      [] > plus /Q.number
        ? > ^
        ? > x /Q.number
      [] > times /Q.number
        ? > ^
        ? > x /Q.number
  bytes.eo: |
    [@] > bytes
lowered: |
  [] > foo
    [x] > pade
      div. > @
        plus.
          times.
            plus.
              x
              6
            x
          12
        plus.
          times.
            plus.
              x
              -6
            x
          12
    pade 0.5 > @
