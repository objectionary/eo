+package org.eolang.custom
+version 0.0.0

[] > creates-object
  QQ.rust > r
    """
    use rand::Rng;
    use eo_env::EOEnv;

    pub fn foo(mut env: EOEnv<'_>) -> EO {
      let mut rng = rand::thread_rng();
      print!("Hello world");
      let i = rng.gen::<i32>();
      i
    }
    """
    *
      "rand= \"0.5.5\""
  TRUE > @

